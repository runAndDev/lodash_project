ideate clamp game plan  

.clamp(number, lowerBound, upperBound )

if (number>lowerBound && number<upperBound) {
    return (lowerBound+upperBound)/2
}
    else if (number<lowerBound) { 
        return lowerBound
    }    
    else if (number>upperBound) { 
        return upperBound 

    }    